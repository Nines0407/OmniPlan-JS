# Project: OmniPlan-JS

## 1. 架构
- monorepo (npm workspaces): Express+WebSocket server (:3000), React+Vite web, MCP Server
- 部署: Docker Compose, nginx(:80) 反向代理 `/api` `/ws` → server:3000, SPA 静态直出
- 运行时: Node 22, better-sqlite3, tsx (不用 node 直跑 dist — ESM 缺 .js 扩展名)
- DaoCloud 镜像加速 `/etc/docker/daemon.json`, dockerd 手动启动

## 2. 状态
- v0.1.0, 三容器全绿 ✅
- 数据库: entrypoint.sh → migrate (user_version pragma) + 空库 seed

## 3. 功能
- 目标: 创建/编辑 name/priority(urgent|high|medium|low)/duration(days), ProjectDetail 卡片 inline 编辑
- 任务: 创建/编辑 name/status/priority/start_date(YYYY-MM-DD)/duration_days(1-365)/progress
  - TaskView 表格: status 下拉、其余字段点按 inline 编辑，切换 status 联动 progress(done→100, 其他→0)
- Timeline 甘特图: 按日精度渲染，列=每天，条宽=duration_days，未设置 start_date 的任务不显示

## 4. 数据链路
新增字段完整链路: migration.sql → shared/types → shared/schemas → server/services → store → page
- `targets.priority TEXT DEFAULT 'medium' CHECK (IN low,medium,high,urgent)`
- `targets.duration INTEGER NULL`
- `tasks.start_date TEXT`, `tasks.duration_days INTEGER DEFAULT 1`
- `day_summary` view (was `week_summary`, migration 003 重建)

## 5. 禁令
- 🚫 不改 `noUncheckedIndexedAccess: true` (路由用 `!` 断言)
- 🚫 不用 `node` 跑 dist, 用 `tsx`
- 🚫 不删 entrypoint.sh / nginx.conf 中 `/ws` `/api` 代理
- 🚫 不删 App.tsx 的 WebSocket subscribe
- 🚫 未登录不处理 WS 事件
- 🚫 priority 不加新值; duration 单位天
- 🚫 不改 handleStatusChange 的 progress 逻辑

## 6. 关键文件
- `deploy.context` 本项目锚点
- `packages/server/entrypoint.sh` 启动入口
- `packages/web/nginx.conf` 反向代理
- `packages/shared/src/types/target.ts` CreateTargetInput/UpdateTargetInput
- `packages/shared/src/schemas/target.ts` CreateTargetSchema/UpdateTargetSchema
- `packages/server/src/services/target-service.ts` createTarget/updateTarget
- `packages/web/src/pages/ProjectDetail.tsx` 目标卡片+创建
- `packages/web/src/pages/TaskView.tsx` 任务表格+创建+inline编辑
- `packages/web/src/stores/targetStore.ts` / `taskStore.ts`
